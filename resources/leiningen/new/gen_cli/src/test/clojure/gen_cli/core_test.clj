(ns {{name}}.core-test
  (:require [clojure.test :refer :all]
            [{{name}}.core :refer :all]))

(deftest {{name}}-core-test
  (testing "TODO: add real test for {{name}}.core-test"
    (is (= 6 (+ 1 2 3)))))
